{
  "name": "trustmymsg-landing",
  "version": "1.0.0",
  "scripts": {
    "start": "python3 -m http.server 3000 --directory public",
    "build": "mkdir -p build && cp public/index.html build/"
  }
}
